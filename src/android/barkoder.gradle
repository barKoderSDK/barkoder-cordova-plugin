repositories {    
    jcenter()
    flatDir {
        dirs 'libs'
    }
}

dependencies {
    implementation (name:'barkoder', ext:'aar')
}

android {
    packagingOptions {
        exclude 'META-INF/NOTICE'
        exclude 'META-INF/LICENSE'
    }
}